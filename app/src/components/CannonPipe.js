import React from 'react';
import { pathFromBezierCurve } from '../utils/fomulas';

const CannonPipe = () => {
    const cannonPipeStyle = {
        fill: '#999',
        stroke: '#666',
        strokeWidth: '2px',
    };
    
    return (
        <g

        />
    );
};

export default CannonPipe;